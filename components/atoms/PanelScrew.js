import { makeStyles } from '@material-ui/core/styles'
import { Box } from '@material-ui/core'

const useStyles = makeStyles(theme => ({
}))

export default function PanelScrew() {
    const classes = useStyles()
    return <Box>screw</Box>
}
